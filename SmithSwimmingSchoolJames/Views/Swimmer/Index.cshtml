﻿<div class="container">
    <div class="row">
        <div class="col-md-7 offset-3">
            <h2>Swimmer Home</h2>
            @{
                if ((bool)ViewData["Registrado"])
                {
                     <p><a asp-action="EditProfile" asp-route-id="@ViewBag.CoachId" class="btn btn-light">Edit Profile</a></p>
                }
                else
                {
                    <p><a asp-action="AddProfile" class="btn btn-light">Add Profile</a></p>
                }
            }
            <p><a asp-action="VerReports" class="btn btn-dark">Ver reports </a></p>

            <p><a asp-action="VerAsistentes" class="btn btn-dark">Ver asistentes a clases</a></p>
        </div>
    </div>
</div>